{
  "name": "ledgerview-backend",
  "version": "1.0.0",
  "description": "LedgerView backend: MongoDB Atlas persistence + Angel One SmartAPI live market data proxy. Indian markets research platform — not a broker.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.6.0",
    "otplib": "^12.0.1"
  },
  "devDependencies": {
    "mongodb-memory-server": "^10.2.0"
  }
}
